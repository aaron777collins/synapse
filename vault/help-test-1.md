# Help Test 1

This links to [[help-test-2]] for cross-referencing.

This is a paragraph about software #testing and #demo purposes. It spans multiple lines to demonstrate how the tag note view extracts full paragraph context, not just the line with the tag.

## Deep Dive

Here we discuss more advanced #testing strategies including integration tests and end-to-end testing patterns. **Bold text** and *italic text* for formatting demo.

## Code Examples

```javascript
function greet(name) {
  const message = `Hello, ${name}!`;
  console.log(message);
  return message;
}
```

```python
def fibonacci(n):
    """Generate fibonacci sequence."""
    a, b = 0, 1
    for _ in range(n):
        yield a
        a, b = b, a + b
```

```bash
echo "Hello World"
grep -r "TODO" src/ | wc -l
```

Some content for search testing. TESTINPUT123 TYPETEST CTRLSTEST

- List item one
- List item two with a [[link]]
- [ ] Checkbox item

> A blockquote for preview testing
